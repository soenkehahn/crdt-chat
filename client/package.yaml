name: patches-client

dependencies:
      - base < 5
      - react-flux
      - servant-client
      - crdt-tree-vector
      - ghcjs-base
      - aeson
      - containers
      - servant
      - transformers
      - ghcjs-prim
      - http-client

ghc-options:
  -Wall

tests:
  spec:
    main: ClientTests.hs
    source-dirs:
      - test
      - ../src
      - src
    dependencies:
      - hspec

executables:
  patches-client:
    main: Main.hs
    source-dirs:
      - src
      - src/exe
      - ../src
